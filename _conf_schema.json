{
  "cache_ttl": {
    "description": "数据缓存有效期（秒）",
    "type": "int",
    "default": 86400,
    "hint": "缓存网页数据的时间，避免频繁请求。设为0则禁用缓存。"
  },
  "card_width": {
    "description": "卡片宽度（像素）",
    "type": "int",
    "default": 450,
    "hint": "单张卡片的宽度。"
  },
  "color_scheme": {
    "description": "颜色主题",
    "type": "string",
    "default": "default",
    "options": ["default"],
    "hint": "目前仅支持 default。"
  }
}